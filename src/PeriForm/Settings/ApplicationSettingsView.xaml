<UserControl x:Class="PeriForm.Settings.ApplicationSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModel="clr-namespace:PeriForm.Infrastructure.ViewModel"
             xmlns:local="clr-namespace:PeriForm.Settings"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:ApplicationSettingsDesignModel, IsDesignTimeCreatable=True}"
             viewModel:ViewModelLocator.WireViewModel="{x:Type local:ApplicationSettingsViewModel}">

    <!-- Content -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <!-- Looks & Feel-->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="32" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Grid.ColumnSpan="2"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0 5 0 0"
                   Text="Looks &amp; Feel" />

            <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   VerticalAlignment="Center"
                   Margin="8"
                   Text="Dark Theme" />
            <CheckBox Grid.Row="1"
                  Grid.Column="1"
                  VerticalAlignment="Center"
                  Margin="5"
                  ToolTip="If enabled the application will use a dark theme instead of the default light theme"
                  IsChecked="{Binding UseDarkTheme}" />
        </Grid>

        <!-- Behaviour-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="32" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Grid.ColumnSpan="2"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0 5 0 0"
                   Text="Behaviour" />

            <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   VerticalAlignment="Center"
                   Margin="8"
                   Text="Minimize On Close" />
            <CheckBox Grid.Row="1"
                  Grid.Column="1"
                  VerticalAlignment="Center"
                  Margin="5"
                  ToolTip="If enabled instead of closing the window will minimize when closed"
                  IsChecked="{Binding MinimizeOnClose}" />
        </Grid>
    </Grid>
</UserControl>